API_HOST=127.0.0.1
API_PORT=4310
WEB_PORT=4311
# LIVE is the default. Set this only for local deterministic demos/tests.
# FIXTURE_MODE=1
FIXTURE_REPLAY_SPEED=1
# The session vault automatically uses the current Windows user's LOCALAPPDATA.
