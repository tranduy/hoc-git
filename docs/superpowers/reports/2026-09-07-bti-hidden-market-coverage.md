# BTI coverage continuation — 2026-09-07, package v3

Status: **REVIEW_READY v3 / WAITING_INTEGRATION / WAITING_LIVE**. Functional/live acceptance is unfinished. This package replaces the first LOCAL_READY checkpoint for review; it does not claim DONE.

Baseline `1710d642136138b536e4e2736a6ccb611e55fd97`, tree `6ceccb6655fd3af4e759f99d39c2d9376ecce2d5`. Private checkout: `F:\0. PROJECT\tool-chenh\.worktrees\parallel-bti-20260907`. Exchange: `F:\0. PROJECT\tool-chenh\.run\parallel-hidden-markets-2026-09-07\bti\v3`. Read assignment01 and revised assignment05 from the running repository. BTI owns investigation, code, private shared wiring and acceptance; the prompt-author reviews/prepares integration, and SABA retains runtime ownership.

## Corrections after rereading the spec

- The new collector is now imported and executed by the actual private `network-observer.ts`. Its delayed-detail regression is a durable source test, not a virtual transform under `.run`. Shared source/tests are separately packaged. Passive duplicate BTI CDP response rejection stays intact; the actual transport is the existing authenticated in-page HTTP collector.
- Detail discovery requires proven native prematch state and excludes IDs present in the live roster. Prematch-to-live transitions remove cached/queued detail and cancel obsolete requests. All advertised league pages still hydrate before authoritative roster replacement; no invented endpoint or label-derived market equivalence was introduced.
- Existing refresh invocations pump due detail without waiting for the next roster sweep. Defaults are 12 seconds for kickoff within six hours or unknown, 60 seconds for more distant matches. Receipt-based deadlines, three lanes, 100ms spacing, timeout and bounded failure backoff remain distinct from the application's 3-second publication rhythm. These numerical defaults are conservative implementation assumptions awaiting real-source validation, not measured freshness guarantees.
- Queue is capped at128 and retained detail at2048 events/24MiB; each body is capped at2MiB. The full discovered roster denominator remains visible and overflow/eviction prevents complete-coverage claims. Queue refill progresses beyond128 instead of starving the remainder.
- Auth/context changes invalidate prior session state and abort list/detail requests; late work cannot restore old cache. Failed roster renewal retains the last committed generation while known detail continues refreshing. Initial/auth-reset failure also obeys the roster retry deadline.
- List request and receipt times are captured at acquisition. A merged partition uses the oldest retained contribution's times. This deliberately makes later pages look older; an early page never gains freshness from a slow later page. Detail preserves each event's actual request/receipt times. Reconnect and cached replay do not restamp old prices, and provider `sourceTimestampMs` stays null.
- Newer authoritative detail omission/empty results suppress older main markets previously owned by detail. Explicit market/event closure, native-family removal and selection-line replacement retain authority until newer affirmative evidence. Shallow main preserves disjoint detail-only selections; newer main updates win against older detail.
- Parser now marks structural handicap output SIGNED, preserving zero lines through the actual canonical reader. Fourteen regressions cover seven native HC codes in roster/detail form, HOME/AWAY orientation and -0.5/0/+0.5. Zero stays BLOCKED by existing no-push comparison policy; nonzero half-lines remain eligible. No shared normalization or comparison rule changed.
- Parser preserves safe numeric IDs, unmapped codes and unnamed native rows without inventing tradeable identities. Unpaired/invalid selections beside a valid pair now get separate exclusion evidence; normalized observations contain only their actual outcomes. Unresolved event identity is explicitly excluded.
- Detail identity hydrates only from compatible same-event authoritative roster data. Valid peers are not discarded because another batch event lacks identity. Unresolved empty shells do not become tombstones. Detail-first reconnect buffering rehydrates when the roster commits while keeping original acquisition clocks.
- Browser fallback cancellation, renewed-launch separation and literal-empty handling from v1 remain. That fallback is not evidence of extension prematch coverage.

## Actual diagnostics caller and discovery probe

`bti-source-inventory.ts` reads current page-owned V10 roster response/cache through the existing `probeBtiPageHealth` -> PAGE_HEALTH -> pipeline diagnostic path. It makes no fetch, storage read, worker callback or DOM action. The observer's real caller is already present; this package supplies its helper import and bounded parsing/transport limits.

The probe deduplicates roster/live/prematch membership across the collector's populated `/live`, empty `/live/initial` and `/prematch/initial` responses. Market, selection and native-type counts cover **detail cache only**. It counts numeric/malformed IDs and successful empty detail. Limits:28MiB total input,2MiB per body,100000 traversal rows,50ms synchronous scan budget and32 displayed safe native codes. The source has236 observed native types, so taxonomy overflow now sets only `nativeTypeCountsTruncated` and continues counting all native rows. Input/time/row limit hits set `nativeInventoryTruncated`; then total counts are partial. Parsing one bounded JSON body is synchronous and can exceed the time budget before the next check. The probe is a bounded cache inventory, not proof of every market the provider could expose.

The previous400-character/old-key rosterCoverage gate discarded richer diagnostics. Extension parsing and the two API consumers now accept the bounded4096-character shape; auth-error invalidation remains active with the larger heartbeat. No publication, socket, coalescing or UI cadence code changed.

## Fresh verification

At22:40 UTC+7, **13 files / 577 tests passed**, zero skipped, maxWorkers1,38.63s. This includes full real `network-observer.test.ts` (344 tests), BTI collector/parser/adapter/browser/cross-boundary tests, full ChromeCatalogDataPlane and pipeline telemetry suites with AP/SABA regressions. API and extension `tsc --noEmit` exited0 after the final implementation changes.

```text
npx.cmd vitest run apps/api/src/providers/bti/bti-direct-catalog.test.ts apps/api/src/providers/bti/bti-direct-catalog.coverage.test.ts apps/api/src/providers/bti/bti-observed-catalog.test.ts apps/api/src/providers/bti/bti-browser-manager.test.ts apps/api/src/chrome-bridge/bti-http-adapter.test.ts apps/api/src/chrome-bridge/bti-http-adapter.collector.test.ts apps/chrome-extension/src/bti-catalog-refresh.test.ts apps/chrome-extension/src/bti-page-health.test.ts apps/chrome-extension/src/bti-source-inventory.test.ts apps/chrome-extension/src/bti-network-observer.test.ts apps/api/src/chrome-bridge/chrome-catalog-data-plane.test.ts apps/api/src/diagnostics/pipeline-telemetry.test.ts apps/chrome-extension/src/network-observer.test.ts --maxWorkers=1 --reporter=dot
npm.cmd run typecheck --workspace apps/api
npm.cmd run typecheck --workspace apps/chrome-extension
```

Regressions were observed failing before the fixes, including source clock inflation, bootstrap retry, real-observer late detail forwarding, authoritative disappearance, batch poisoning, reconnect identity hydration and oversized diagnostic rejection. Old observer fixtures were updated to explicitly prove prematch and advance source time when expecting another TTL-governed request; a partial failed roster now asserts no publication. Independent code reviews found the last receipt/backoff/live-inventory and reconnect issues and verified their corrections.

Synthetic cross-boundary evidence: hidden price0.90->0.75 changes after an actual second simulated detail acquisition, main price stays0.82, unmapped rows persist, and receiver reconnect preserves old quote age. This is not a live provider quote or measured latency.

## Source access evidence and remaining gates

The read-only grant for build `sha256:e9e86f1c5d0cd322d06447a37f9bb247b6a03abbec92f19c707ea06f56e45cf8`, source `chrome:BTI:2105829043`, tab2105829043 named epoch`:7`. At approximately21:58 the identity check returned`:9`; reads stopped before catalog access. The renewed22:09:54.948–22:19:54.948 grant named`:9`. At22:15:14.102 (`nowMs1788794114102`) the next identity check returned`:11`; reads stopped again before catalog access. The build/source/tab matched. These are boundary diagnostics, not accepted coverage samples. No windows are pooled.

A segmented same-source/tab/build discovery grant was subsequently recorded22:21:35.407?22:31:35.407 and used below. It explicitly allowed automatic epoch rotations as independently fenced segments. The sampler ended at expiry; there is no continuing source-access grant. `verification/bti-read-catalog.mjs` is a ready bounded GET sampler: checks build/source/tab/epoch before and after catalog reads, discards cross-boundary responses, retains only BTI inventory/accounting/quote changes, and stops at grant expiry. Its optional follow-epoch mode requires explicit ledger permission; the script itself grants nothing.

## Aligned live API discovery (deployed .39; not this package)

Used the newly recorded segmented grant with before/after build/source/tab/epoch fences around every catalog response. Sampler process22:27:25.800?22:31:35.407 UTC+7 ended WINDOW_ENDED. All69 accepted samples stayed in epoch74040073-8f28-489c-b83f-5d6f442faf05:14, first sample22:27:29.225, last22:31:32.727 (243502ms). Zero cross-epoch samples or runtime mutations.

First sample164 events/3169 markets/6338 quotes/8216 native observations; final162/3186/6372/8256. Events include the account's live and prematch catalog, not a proven prematch-only provider denominator. Across13 API revisions,1712 quote-value/status/line change observations involved861 distinct selection identities. These are observations between polls, not every source tick. Actual poll gaps p50=3392ms,p95=4880ms,max=5513ms include identity fences and response time; these are NOT source-to-ingest/API/UI latency figures.

First native accounting:3287 NORMALIZED,1759 EXCLUDED,3170 UNMAPPED,236 types. Codes are retained for investigation rather than guessed from labels. Published first-sample markets included204 corner markets,39 card handicap/total markets and9 sending-off markets. Repeated price changes occurred in BTTS, team totals and win-both-halves as well as main goals. Example selection0QA882291998374359103Q0Q1 (event882291997418033152, SH_BTTS) changed -0.382->-0.433->-0.365->-0.407 within the same segment. No corner/card quote change was observed in this short window; that absence alone does not establish a refresh defect. The code path implies these non-main groups came from detail, but API data alone is not an independent endpoint/body comparison.

Source-driven correction:3287 normalized observation identities are unique, yet only3169 markets published. The118-count difference matches zero-handicap observations exactly: HC0/HC39=40,HC1=56,HC2=5,HC619=3,HC14=10,HC10=4. Local canonicalHomeHandicap rejects zero unless handicapLineFormat is SIGNED; BTI signed lines omitted that flag. This grounds the v3 parser/canonical regression without changing shared normalization or allowing refund lines into no-refund comparison. Identity-level deployed confirmation still requires the integrated build and fresh paired source/catalog evidence. The regression reached the actual observed catalog and comparison mapper: all14 cases failed before the SIGNED flag and passed after it.

Evidence artifact and guarded sampler are in the BTI exchange: discovery-20260907-2227.json. It contains only authorized BTI API fields; no credentials or raw provider response/session capture. v2 remains immutable historical review output; v3 adds the source-driven zero-line correction and taxonomy-overflow correction. This four-minute discovery is not the requested10-15minute integrated acceptance and provides no24-hour operational evidence.

The later22:33?23:03 non-protected API grant added runtime-package provenance. A supplementary identity check before a new paired zero-handicap sample found adapters dist hash05ecf024cf7a9c94b0de32f085a762b9bcd6f38f0eeb5c6a20419089351b5a0b, differing from the grant's3f2060911b14c2ff899cd892b286151771d3d893dda869c2f7402377388f6a45. Core matched47ed3a170407ededd6abfc634fb0368fc8404fb2a47413428e8e07be167f7612. The operation stopped BEFORE any API request; no new sample was retained. Root was allowed to build during that non-protected interval. A fresh complete build/package identity is needed before further reads; coordinator build hash alone does not cover adapters/core. The earlier protected-window evidence is kept separate with the runtime owner's no-rebuild statement.

Still required before DONE:

1. Review/integrate this concrete provider/shared package, deploy only under the runtime owner's authority, and publish its exact build/epoch. Private code must not be deployed wholesale from the synthetic WIP baseline.
2. Use current real-source discovery and the bounded probe to establish roster/detail/native denominators and inspect gaps outside the existing observed endpoints. Cache counts alone do not prove full-provider completeness; corners/cards/native signatures require actual data.
3. On the published build, measure genuine repeated hidden price/line/status changes alongside existing markets through the current ingest/revision/API/UI flow. API polling can bound observations but cannot invent provider-origin or UI receipt times. No live source/ingest/API/UI latency percentiles have been obtained yet.
4. Verify reconnect/session recovery within a separately granted operation scope and record runtime duration/failures. No session reset, reload, navigation, debugger claim, capture-filter change or shared startup occurred. A10–15minute window is limited evidence, not24-hour operation.

Realtime remains ChromeCatalogDataPlane -> CatalogRevisionStore -> `/api/realtime` -> CatalogRevisionCoordinator -> catalog/UI/comparison worker, with existing minimumPublishIntervalMs/fallbackMs3000 unchanged. No hidden freshness requirement was relaxed to declare success. Runtime coverage, accuracy, hidden-price continuity and AP/SABA live acceptance remain unverified.
